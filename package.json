{
  "name": "ddd-patterns",
  "version": "1.0.0",
  "main": "index.js",
  "license": "MIT",
  "lint-staged": {
     "*.ts": "tslint --fix"
  },
  "scripts": {
    "build": "tsc",
    "lint": "tslint src/**/*.ts{,x}",
    "lint:fix": "tslint src/**/*.ts{,x} --fix",
    "prepare": "husky install"
  },
  "devDependencies": {
    "husky": "^8.0.1",
    "lint-staged": "^13.0.3",
    "tslint": "^6.1.3",
    "typescript": "^4.8.3"
  }
}
